# Scripts

Planned scripts:

- `seed_enterprise.py` - seed departments, agent cards, policies, tools, and synthetic ACME data
- `demo_reset.py` - reset deterministic demo state
- `deploy.ps1` - deploy backend to Cloud Run
- `shutdown.ps1` - scale Cloud Run safely after demo recording

